# AGENTS.md

This host-project root file is a stable pointer, not a synchronized copy of the
agent bundle policy.

Agents must read and strictly follow `.agents/AGENTS.md` before doing any
implementation, documentation, git, sync, or publication work in this
repository.

Do not mirror `.agents/AGENTS.md` into this file. Changes inside `.agents/` do
not require edits here unless the canonical policy path itself changes.
